# Brainrot Academy v0.14.2 — GitHub APK Tracking Fix

- Fixed `.gitignore` so only the generated root `/android/` project is ignored.
- `resources/android/` launcher icons are now tracked by Git and available to GitHub Actions.
- Added a regression test for the GitHub-only icon resource failure.
- Android version metadata and APK artifact names updated to v0.14.2 / versionCode 1402.
